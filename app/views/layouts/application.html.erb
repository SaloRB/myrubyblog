<!DOCTYPE html>
<html>
  <head>
    <title>Myrubyblog</title>
    <%= csrf_meta_tags %>

  </head>

  <body>
  	<% flash.each do |key, value| %>
  		<p><%= value %></p>
  	<% end %>
    <%= yield %>
  </body>
</html>
